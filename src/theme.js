import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#ad1457",
      secondary: "#d1c4e9",
      base: "#fff",
    },

    secondary: {
      main: "#ffc107",
      secondary: "#4527a0",
      base: "#000",
    },
  },

  fonts: {
    Nunito: "Nunito, sans-serif",
  },
});
