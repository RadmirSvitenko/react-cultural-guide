import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#ad1457",
      secondary: "#d1c4e9",
    },

    secondary: {
      main: "#ffc107",
      secondary: "#4527a0",
    },
  },

  fonts: {
    Nunito: "Nunito, sans-serif",
  },
});
